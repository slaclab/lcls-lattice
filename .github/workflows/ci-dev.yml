name: TestsDev

on:
  push:

jobs:
  build:
    runs-on: "ubuntu-latest"
    steps:
      - uses: actions/checkout@v2
      - uses: conda-incubator/setup-miniconda@v2
        with:
          python-version: 3.9
          miniforge-variant: Mambaforge 
          channels: conda-forge
          channel-priority: true
          
      - name: Install dependencies
        run: |
          mamba install pytest pytao bmad

      - name: Fetch executables for testing
        uses: slaclab/accel-test-exec-fetcher@v1.1

      # This step can be removed, just ensuring it is working properly
      - name: Check mad8s version
        run: mad8s --version
        shell: bash
          
      - name: Run tests
        shell: bash -l {0}
        run: |
          conda activate base
          export LCLS_LATTICE=$GITHUB_WORKSPACE
          #pytest python/tests/ -v    
          tests/sc_bsyd.bash
          #$LCLS_LATTICE/
          #echo $LCLS_LATTICE
          #cd $LCLS_LATTICE/mad
          #echo "running mad8s"
          #../mad8s < CI-Testing.mad8
          #echo $?
          #echo "testing is .twiss created"
          #test -f SC_BSYD_GUN_CI.twiss 
          #echo $?

          #cd $LCLS_LATTICE/bmad/models/sc_bsyd
          #pwd
          #ls -ls ../../
          #echo "running lc_unit_test_bmad"
          #../../../lc_unit_test_bmad sc_bsyd.lat.bmad
          #echo "testing is Bmad twiss.out created"
          #test -f twiss.out
          #echo $?

        
