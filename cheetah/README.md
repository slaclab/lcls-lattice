# Check cheetah lattices into repo

1. Submit PR to github.

2. Generate new lcls-lattice release.

3. Update `/sdf/group/ad/sw/scm/repos/optics/lcls-lattice` on s3df.

4. Update prod using zip of release at `lcls-srv01:/usr/local/lcls/tools/lcls-lattice`