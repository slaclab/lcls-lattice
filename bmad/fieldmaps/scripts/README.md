fit_zscan.py is used to produce a Bmad cartesian field map from zscan data.  For large zscan files, it is recommended to run this script on an MPI cluster.
evaluate_map.py will calculate untulator integrals of a Bmad cartesian field map and produce a field.out file containing By vs z for the map.
